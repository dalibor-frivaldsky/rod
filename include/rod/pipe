#pragma once

#include <rod/concept/CallableWith>
#include <utility>


namespace rod { namespace operators {

	template< typename Left, CallableWith< Left > Fn >
	decltype(auto) operator | (Left&& left, Fn&& fn) {
		return fn(std::forward<Left>(left));
	}

}}
